# Godot 4+ 임포트 캐시 — 머신마다 재생성되므로 커밋하지 않는다
.godot/

# 내보내기 산출물
/build/
export_presets.cfg

# 검증용 스크린샷 — 매번 재생성되므로 커밋하지 않는다
/_screenshots/

# 참고용 원작 스크린샷 — 상용 게임 저작물이므로 저장소에 넣지 않는다
/refs/

# 하네스 중간 산출물 (설계·검증 리포트)는 추적한다 — _workspace/는 제외하지 않음

# macOS
.DS_Store
